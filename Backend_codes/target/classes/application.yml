server:
  port: 8080

spring:
  application:
    name: personal-ai-backend
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:abbanadurgaprasad9390@gmail.com}  # Use environment variable or set here
    password: ${MAIL_PASSWORD:nmiofxzlgcallpqq}      # Use environment variable for security
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

cors:
  allowedOrigins:
    - "http://localhost:4200"
    - "http://localhost:63535"
    - "http://127.0.0.1:4200"
    - "http://127.0.0.1:63535"

ai:
  profileUrls:
    - "https://your-portfolio-site.com"
    - "https://github.com/your-username"

  ollama:
    baseUrl: "http://localhost:11434"
    model: "llama3"

# Email Configuration
mail:
  from: "abbanadurgaprasad9390@gmail.com"  # Your email address (sender)
  to: "abbanadurgaprasad9390@gmail.com"      # Your email address (recipient for notifications)
  subject:
    prefix: "Portfolio Contact Form: "

# Image Upload Configuration
app:
  upload:
    dir: "uploads"  # Directory for storing uploaded images (relative to project root)


